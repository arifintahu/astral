Run the test suite.

1. `cargo test` — Rust unit and integration tests (a run with 0 tests is acceptable while the test suite is being built out)
2. Frontend: no test script is configured yet; add a `"test"` entry to web/package.json when frontend tests are introduced
